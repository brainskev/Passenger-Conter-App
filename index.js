
//initialize the count as 0
//listen for clicks on the increament button
//icreament variable when button is clicked
//change the count-el in the HTML to reflect the new count

let countEl = document.getEmentById("count-el")

let count = o

function increment() {
    count = count + 1
    countEl.innerText = count                                            

}
funtion SVGAElement() {
    console.log

}
